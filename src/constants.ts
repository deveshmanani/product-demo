export const ROUTES = {
  HOME: '/',
  PRODUCTS: '/products',
  SEND: '/send',
} as const;

export const PAGINATION = {
  PAGE_SIZE: 8,
} as const;

export const PRICE_RANGE = {
  MIN: 0,
  MAX: 500,
} as const;
