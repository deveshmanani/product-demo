import { Typography } from 'antd';

const { Title } = Typography;

export default function ProductListPage() {
  return (
    <div>
      <Title level={2}>Product List</Title>
      <p>Product table and filters will be implemented in Phase 2.</p>
    </div>
  );
}
