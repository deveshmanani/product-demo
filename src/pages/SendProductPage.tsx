import { Typography } from 'antd';

const { Title } = Typography;

export default function SendProductPage() {
  return (
    <div>
      <Title level={2}>Send Item</Title>
      <p>Product grid and send flow will be implemented in Phase 3.</p>
    </div>
  );
}
